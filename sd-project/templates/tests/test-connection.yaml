apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sd-project.fullname" . }}-test-connection"
  labels:
    {{- include "sd-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sd-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
